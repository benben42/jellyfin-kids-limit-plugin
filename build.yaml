---
# Metadata for building/packaging with jprm (Jellyfin Plugin Repository Manager).
# See https://github.com/oddstr13/jellyfin-plugin-repository-manager
name: "Kids Watch-Time Limit"
guid: "a1e5c7f2-3b4d-4e6a-9c8b-2d1f0e3a5b7c"
version: "1.0.0.5"
targetAbi: "10.11.11.0"
framework: "net9.0"
overview: "Cumulative daily watch-time limits for kids."
description: >
  Enforces cumulative daily watch-time limits for kids: per-user, per-weekday,
  per-time-of-day windows, with parent bonus time, a REST API, and a dashboard.
  Observes playback via ISessionManager and stops the session when a limit is
  reached. Never mutates account or library state.
category: "General"
owner: "benben42"
artifacts:
  - "Jellyfin.Plugin.KidsLimit.dll"
